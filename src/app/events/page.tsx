import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Events & Conferences', description: 'Upcoming ministry events, healing conferences, and live broadcasts.' };
export { default } from './EventsClient';
