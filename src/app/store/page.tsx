import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Resource Store', description: 'Books, audio, video, and study materials from AWM Ministries.' };
export { default } from './StoreClient';
